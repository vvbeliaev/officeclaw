import { addCollection } from '@iconify/svelte';
import ocIcons from './oc.json';

// Register the OfficeClaw custom icon collection
addCollection(ocIcons);

export { default as Icon } from '@iconify/svelte';
